# Copy this file to infra/agent_registry.yaml and customise the URLs
# so the global AI sandbox can route requests to each agent.
agents:
  credit_appraisal:
    url: "http://localhost:8091"
    version: "1.0.0"
    repo: "https://github.com/Sherlock2019/credit-appraisal-agent-poc.git"
  asset_appraisal:
    url: "http://localhost:8092"
    version: "1.0.0"
    repo: "https://github.com/Sherlock2019/asset-appraisal-agent-.git"
